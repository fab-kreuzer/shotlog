services:
  app:
    build: .
    container_name: shotlog-app
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: prod
    networks:
      - database_db_net

networks:
  database_db_net:
    external: true
